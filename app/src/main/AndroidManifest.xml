<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:roundIcon="@drawable/just_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shedulytic"
        tools:targetApi="31">

        <activity
            android:name=".NetworkTestActivity"
            android:exported="true" />
        <activity
            android:name=".HomepageActivity"
            android:exported="true" />
        <activity
            android:name=".MainNavigationActivity"
            android:exported="true" />
        <activity
            android:name=".SignupActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true" />
        <activity
            android:name=".AuthorizationActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity5"
            android:exported="false" />
        <activity
            android:name=".MainActivity4"
            android:exported="false" />
        <activity
            android:name=".MainActivity3"
            android:exported="false" />
        <activity
            android:name=".MainActivity2"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TimelineActivity" android:exported="true" />
        <activity android:name=".AddTaskActivity" />
        <activity android:name=".AddHabitActivity" />          <!-- New activities for habit verification -->
        <activity android:name=".PomodoroActivity" android:exported="false" />
        <activity android:name=".LocationVerificationActivity" android:exported="false" />        <!-- Notification Activities -->
        <activity android:name=".NotificationSettingsActivity" 
                  android:exported="false"
                  android:parentActivityName=".MainActivity"
                  android:theme="@style/Theme.Shedulytic" />
        <activity android:name=".NotificationHandler$ExtendTaskActivity" 
                  android:exported="false"
                  android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".NotificationHandler$CantCompleteTaskActivity" 
                  android:exported="false"
                  android:theme="@android:style/Theme.Dialog" />

        <!-- Reward Popup Activity -->
        <activity 
            android:name=".RewardPopupActivity"
            android:exported="false"
            android:theme="@style/Theme.Shedulytic.Transparent" />

        <!-- Snooze Picker Activity -->
        <activity 
            android:name=".SnoozePickerActivity"
            android:exported="false"
            android:theme="@style/Theme.Shedulytic.Transparent"
            android:excludeFromRecents="true" />
        
        <!-- Task Completion Activity (handles completion from notification) -->
        <activity 
            android:name=".TaskCompletionActivity"
            android:exported="false"
            android:theme="@style/Theme.Shedulytic.Transparent"
            android:excludeFromRecents="true" />

        <!-- Notification Receivers -->
        <receiver android:name=".NotificationHandler$NotificationReceiver" android:exported="false" />
        <receiver android:name=".NotificationHandler$TaskActionReceiver" android:exported="false" />
        
        <!-- Reminder Notification Receivers -->
        <receiver android:name=".ReminderNotificationManager$ReminderReceiver" android:exported="false" />
        <receiver android:name=".ReminderNotificationManager$ReminderActionReceiver" android:exported="false">
            <intent-filter>
                <action android:name="com.simats.schedulytic.COMPLETE_TASK" />
                <action android:name="com.simats.schedulytic.CANT_COMPLETE" />
                <action android:name="com.simats.schedulytic.SNOOZE_TASK" />
                <action android:name="com.simats.schedulytic.DISMISS_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver to reschedule notifications after device restart -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Additional Notification Receivers -->
        <receiver android:name=".HabitReminderReceiver" android:exported="false" />
        <receiver android:name=".HabitCompletionReceiver" android:exported="false" />
        <receiver android:name=".BootCompletedReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
